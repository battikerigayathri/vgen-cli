fields:
  - key: lineItems
    label: Line items
    type: array
    bag: inputs
    requiredFromStage: collect_line_items

  - key: reviewConfirmed
    label: Review confirmed
    type: boolean
    bag: inputs
    requiredFromStage: review_summary
