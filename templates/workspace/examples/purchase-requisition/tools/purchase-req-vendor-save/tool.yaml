# Leave id empty; the CLI writes back the assigned ID on push (see docs/id-lifecycle.md)
id: ""
name: Purchase Req Vendor Save
description: Saves vendorId to workflow inputs via workflowPatch after HITL submit.
type: JS
input_modes:
- text
output_modes:
- application/json
systemInstructions:
- Call ONLY after the user submits purchase-req-vendor-form HITL.
- NEVER call on the same turn as Purchase Req Vendor HITLConfig.
- Returns workflowPatch.inputs with vendorId — platform auto-advances when doneWhen satisfied.
arguments:
  vendorId:
    description: Vendor ID from HITL submit
    required: true
    type: string
examples: []
tags:
- purchase-requisition
- workflow
createdBy: ''
managedBy: []
version: '1.0'
