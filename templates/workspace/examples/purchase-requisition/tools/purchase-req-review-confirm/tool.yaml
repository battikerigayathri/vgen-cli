# Leave id empty; the CLI writes back the assigned ID on push (see docs/id-lifecycle.md)
id: ""
name: Purchase Req Review Confirm
description: Sets reviewConfirmed on workflow inputs after review HITL submit.
type: JS
input_modes:
- text
output_modes:
- application/json
systemInstructions:
- Call ONLY after the user submits purchase-req-review HITL with reviewConfirmed true.
- NEVER call on the same turn as Purchase Req Review HITLConfig.
- Returns workflowPatch.inputs only — platform advances to terminal when complete.
arguments:
  reviewConfirmed:
    description: Must be true to confirm review
    required: true
    type: boolean
examples: []
tags:
- purchase-requisition
- workflow
createdBy: ''
managedBy: []
version: '1.0'
